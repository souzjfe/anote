import { useState } from 'preact/hooks'
import './Popup.css'
import Editor from '../components/Editor'

export const Popup = () => {
  return (
    <main>
      <Editor />
    </main>
  )
}

export default Popup
